"""Command line helpers shipped with POASTA.

The package contains small utilities that operate on POASTA output:

* ``poasta_graphviz_region.py`` – extract and highlight a sequence region from a
  GraphViz DOT file produced by ``poasta view``.
* ``poasta_plot.py`` – plot dynamic programming matrices and graph layouts for
  debugging purposes.
"""
